(ns game.music
  (:use [engine.core :only (initialize)])
  (:import org.newdawn.slick.Music))

;(initialize
;  (doto
;    (Music. "sounds/dungeon1.xm" true)
;    (.setVolume (float 1))
;    (.loop)))

